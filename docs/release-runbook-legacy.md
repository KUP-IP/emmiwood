# Retired KUP-coupled release runbook

This document previously contained commands targeting KUP Solutions infrastructure. Those commands are intentionally removed because Emmiwood is now a standalone repository.

Use `docs/RELEASE.md`. Historical content remains available in Git history for incident provenance only.
